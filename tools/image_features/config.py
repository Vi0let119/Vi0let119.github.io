"""
图片特征提取系统 — 配置文件
修改此文件即可调整模型、标签池、路径等参数
"""
import os

# ============================================================
# 路径配置
# ============================================================
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
PROJECT_DIR = os.path.dirname(os.path.dirname(BASE_DIR))  # 项目根目录
IMG_DIR = os.path.join(PROJECT_DIR, "图库")

FEATURES_JSON = os.path.join(BASE_DIR, "picture_features.json")
UMAP_JSON = os.path.join(BASE_DIR, "umap_2d.json")
LAST_PROCESSED = os.path.join(BASE_DIR, "last_processed.json")
THUMBNAILS_DIR = os.path.join(BASE_DIR, "thumbnails")
VISUALIZE_DIR = os.path.join(BASE_DIR, "visualize")

# ============================================================
# 模型配置
# ============================================================
YOLO_MODEL = "yolov8n.pt"       # ultralytics 自动下载到本地
CLIP_MODEL = "ViT-B/32"          # 512 维嵌入，~600MB
DEVICE = "cuda"                  # "cuda" 或 "cpu"

# ============================================================
# 处理参数
# ============================================================
THUMBNAIL_SIZE = (200, 200)       # 缩略图尺寸
KMEANS_K = 5                      # 提取的主色调数量
YOLO_CONF_THRESHOLD = 0.35       # YOLO 检测置信度阈值
SCENE_CONFIDENCE_THRESHOLD = 0.08 # 场景标签最低置信度（CLIP 零样本置信度偏低，适当调低）
MAX_IMAGE_DIM = 1024              # 处理时最大尺寸（加速）

# ============================================================
# 支持的图片格式
# ============================================================
SUPPORTED_EXTS = {'.jpg', '.jpeg', '.png', '.webp', '.bmp'}

# ============================================================
# 场景分类候选标签池（可自由扩展）
# ============================================================
SCENE_LABELS = [
    # ── 自然风光 ──
    "日落/夕阳",
    "蓝调时刻",
    "花卉植物",
    "高山/山峰",
    "海洋/水面",
    "森林/树林",
    "雪景",
    "云雾",
    "草原/田野",
    "沙漠/戈壁",
    "星空/夜空",
    "彩虹",
    "河流/瀑布",
    "湖泊/倒影",
    "秋叶/红叶",
    "天空/云彩",
    "水面波纹",

    # ── 城市人文 ──
    "城市建筑/街道",
    "人物肖像",
    "夜景/灯饰",
    "室内空间",
    "美食/饮品",
    "市集/摊位",
    "桥梁/建筑结构",
    "街景/道路",
    "铁路/车站",
    "城市夜景",

    # ── 动物 ──
    "野生动物",
    "宠物猫狗",
    "鸟类",
    "昆虫/蝴蝶",
    "水生动物",

    # ── 近摄/特写（拆分原来的"微距/特写"）─
    "花瓣/花蕊特写",
    "绿叶/植被特写",
    "食物摆盘近摄",
    "静物/摆件",
    "建筑纹理/细节",
    "抽象/纹理",

    # ── 摄影风格 ──
    "黑白摄影",
    "长曝光/光轨",
    "剪影/逆光",
    "微距/特写",
    "倒影/对称",
    "极简/留白",
    "光斑/散景",
]

# ============================================================
# UMAP 降维参数
# ============================================================
UMAP_N_NEIGHBORS = 15
UMAP_MIN_DIST = 0.1
UMAP_RANDOM_STATE = 42
